//Have the api calls be built in a custom function
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || '';

// export async function apiBuilder(path: string, params: Array<string> = []) {
//     //Build the path:
//     let ReqUrl = 
// };